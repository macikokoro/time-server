# JSON API Time Server / http://expressjs.com/
===============================================

* Express API with a route that displays local time.
* Also has a route that takes a name as part of the url and greets by that name.
